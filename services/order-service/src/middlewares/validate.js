/**
 * TODO 6.4.3: Request Validation Middleware
 *
 * Requirements:
 * 1. Validate request body, query, and params against Zod schemas asynchronously.
 * 2. Map issues to formatted error details array.
 * 3. Forward ValidationError to next() upon failure.
 * 4. Attach sanitized data to req.validatedData upon success.
 */
