/**
 * TODO 6.4.6: Order Express Routes
 *
 * Requirements:
 * 1. POST /api/v1/orders - createOrder (authenticate, validate(createOrderSchema))
 * 2. GET /api/v1/orders - listOrders (authenticate, validate(queryOrdersSchema))
 * 3. GET /api/v1/orders/:id - getOrder (authenticate, validate(orderIdParamSchema))
 * 4. PATCH /api/v1/orders/:id/cancel - cancelOrder (authenticate, validate(orderIdParamSchema), validate(cancelOrderSchema))
 */
